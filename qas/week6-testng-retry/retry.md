### TestNG Retrying

TestNG automatically generates an `XML` file (`testng-failed.xml`) pertaining to failed tests that can then be ran to manually retry tests.

You can also automatically retest by using the `IRetryAnalyzer` interface.